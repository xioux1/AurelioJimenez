# AurelioJimenez

This repository contains the code for the AeroClub recsys project. Tests are provided using pytest.

## Installation

Use `pip` to install the runtime and development dependencies:

```bash
python -m pip install -r requirements.txt
```

After installing the requirements you can run the test suite with:

```bash
pytest
```
